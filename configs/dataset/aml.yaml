# AML 数据集配置
data:
  dataset: "aml"
  data_path: "dataset.csv"
  preprocessed_path: "preprocessed_data.pt"
  regenerate: false
  val_ratio: 0.1
  test_ratio: 0.1
  use_smote: false
  smote_ratio: 1.0
  batch_size: 32
  num_workers: 0
