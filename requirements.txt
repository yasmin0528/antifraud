# ============================================================
# AML Transaction Monitoring Model - Dependencies
# ============================================================
# 安装方式: pip install -r requirements.txt
# Python版本要求: >= 3.9

# ==================== 核心依赖 (Core) ====================

# 深度学习框架
torch>=2.0.0

# 数值计算
numpy>=1.24.0

# 数据处理
pandas>=2.0.0

# YAML配置文件解析
pyyaml>=6.0

# 机器学习指标与评估
scikit-learn>=1.3.0

# 可视化
matplotlib>=3.7.0

# HTTP请求 (LLM API调用)
requests>=2.31.0

# 训练日志 (TensorBoard)
tensorboard>=2.14.0

# ==================== 可选依赖 (Optional) ====================

# 不平衡数据集处理 (SMOTE过采样)
# 仅在配置中 use_smote=true 时需要
imbalanced-learn>=0.11.0

# HuggingFace Transformers (本地LLM推理)
# 仅在配置中 use_api=false 时本地加载LLM时需要
# 同时会依赖 accelerate, bitsandbytes
transformers>=4.36.0

# 模型图可视化
# 仅在配置中 plot_model_graph=true 时需要
torchview>=0.2.3
